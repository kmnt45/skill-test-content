## Как работает `useEffect` и какие у него зависимости?

**`useEffect`** — хук для побочных эффектов (запросы, подписки).

**Синтаксис:**
```jsx
useEffect(() => {
  // Код эффекта
  return () => { /* cleanup */ }; // Опционально
}, [dependencies]); // Массив зависимостей
```